{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "npm run seed 2>/dev/null; node server.js",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 100
  }
}
